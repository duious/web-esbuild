/**
 * 增加localStorage键值
 * @static
 * @since 0.0.1
 * @param {string} key 键名
 * @param {any} val 键值
 * @return {boolean} true成功/false失败
 * @example
 * sLocalStorage.setItem('key', 'value')
 */
declare const setItem: (key: string, val: any) => boolean;
/**
 * 删除localStorage键值
 * @static
 * @since 0.0.1
 * @param {string} key 键名
 * @return {boolean} true成功/false失败
 * @example
 * sLocalStorage.delItem('key')
 */
declare const delItem: (key: string) => boolean;
/**
 * 获取localStorage键值
 * @static
 * @since 0.0.1
 * @param {string} key 键名
 * @return {null | string | number | boolean}
 * @example
 * sLocalStorage.getItem('key')
 */
declare const getItem: (key: string) => null | string | number | boolean;
/**
 * 检查localStorage键值是否存在
 * @static
 * @since 0.0.1
 * @param {string} key 键名
 * @return {boolean} true存在 false不存在
 * @example
 * sLocalStorage.isItemExist('key')
 */
declare const isItemExist: (key: string) => boolean;
/**
 * 清理localStorage全部键值
 * @static
 * @since 0.0.1
 * @return {boolean} true成功/false失败
 * @example
 * sLocalStorage.clear()
 */
declare const clear: () => boolean;
/**
 * 获取localStorage已存储键数量
 * @static
 * @since 0.0.1
 * @return {number} localStorage已存储键数量
 * @example
 * sLocalStorage.getLength()
 */
declare const getLength: () => number;
/**
 * 是否已超出最大存储长度
 * @static
 * @since 0.0.1
 * @param {?string} key 键名
 * @param {?string} val 键值
 * @returns {boolean} true已满 false未满
 * @example
 * sLocalStorage.isFull('key', 'value')
 * sLocalStorage.isFull()
 */
declare const isFull: (key?: string, val?: any) => boolean;
export { setItem, delItem, getItem, clear, getLength, isItemExist, isFull };
declare const _default: {
    setItem: (key: string, val: any) => boolean;
    delItem: (key: string) => boolean;
    getItem: (key: string) => string | number | boolean;
    clear: () => boolean;
    getLength: () => number;
    isItemExist: (key: string) => boolean;
    isFull: (key?: string, val?: any) => boolean;
};
export default _default;
