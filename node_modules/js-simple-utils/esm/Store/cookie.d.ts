/**
 * @module sCookie
 * @since 0.0.1
 */
/**
 * 设置cookie键值
 * @static
 * @since 0.0.1
 * @description 时间默认1天
 * @param  {string} key cookie名称
 * @param  {string} val cookie值
 * @param  {?number} [expires=1 * 24 * 60 * 60] cookie过期时间 单位秒 默认1天过期
 * @param  {?string} [path='/'] cookie路径
 * @param  {?string} [domain=''] cookie域名
 * @param  {?boolean} [secure=''] cookie安全标识
 * @param  {?('' | 'strict' | 'lax')} [sameSite=''] cookie同源标识
 * @return {boolean} true成功/false失败
 * @example
 * sCookie.setItem('name', 's')
 * sCookie.setItem('name', 's', 1 * 24 * 60 * 60)
 * sCookie.setItem('name', 's', 1 * 24 * 60 * 60, '/')
 */
declare const setItem: (key: string, val: string, expires?: number, path?: string, domain?: string, secure?: boolean, sameSite?: string) => boolean;
/**
 * 获取cookie键值
 * @static
 * @since 0.0.1
 * @param  {string} key cookie键名
 * @return {undefined | string} cookie值
 * @example
 * sCookie.getItem('name')
 */
declare const getItem: (key: string) => undefined | string;
/**
 * 删除cookie键值
 * @static
 * @since 0.0.1
 * @param  {string} key cookie键名
 * @return {boolean} true成功/false失败
 * @example
 * sCookie.delItem('name')
 */
declare const delItem: (key: string) => boolean;
export { setItem, getItem, delItem };
declare const _default: {
    setItem: (key: string, val: string, expires?: number, path?: string, domain?: string, secure?: boolean, sameSite?: string) => boolean;
    getItem: (key: string) => string;
    delItem: (key: string) => boolean;
};
export default _default;
