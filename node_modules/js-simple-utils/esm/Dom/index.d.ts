/**
 * @module sDom
 * @since 0.0.3
 * @description sDom模块, 提供DOM相关处理
 * @example
 * // first import sDom
 * import { sDom } from 'js-simple-utils'
 * // then use sDom
 * onClick={sObject.stopPropagation}
 * onClick={sObject.preventDefault}
 * onClick={sObject.stopAndPrevent}
 */
/**
 * 阻止事件冒泡
 * @static
 * @since 0.0.3
 * @param {Event} e 事件对象
 * @returns {void}
 * @example
 * sDom.stopPropagation(e)
 */
declare const stopPropagation: (e: Event) => void;
/**
 * 取消事件默认行为
 * @static
 * @since 0.0.3
 * @param {Event} e 事件对象
 * @returns {void}
 * @example
 * sDom.preventDefault(e)
 */
declare const preventDefault: (e: Event) => void;
/**
 * 阻止事件冒泡+取消事件默认行为
 * @static
 * @since 0.0.3
 * @param {Event} e 事件对象
 * @returns {void}
 * @example
 * sDom.stopAndPrevent(e)
 */
declare const stopAndPrevent: (e: Event) => void;
declare const _default: {
    stopPropagation: (e: Event) => void;
    preventDefault: (e: Event) => void;
    stopAndPrevent: (e: Event) => void;
};
export default _default;
export { stopPropagation, preventDefault, stopAndPrevent };
