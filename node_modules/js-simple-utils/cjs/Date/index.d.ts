/**
 * 日期解析
 * @static
 * @param date
 * @param utc
 * @returns {Date}
 */
declare const parseDate: (date: undefined | null | string | number | Date, utc?: unknown) => Date;
/**
 * 日期格式化
 * @static
 * @since 0.0.2
 * @param {?string} date 待格式化的日期
 * @param {?string} template 格式化模版
 * @param {?string} loc 语言
 * @returns {string} 格式化后的日期
 * @example
 * sDate.format(new Date(), 'YYYY-MM-DD') // => '2020-01-01'
 */
declare const format: (date?: Date | string | number | undefined, template?: string, loc?: string) => string;
declare const _default: {
    parseDate: (date: string | number | Date, utc?: unknown) => Date;
    format: (date?: string | number | Date, template?: string, loc?: string) => string;
};
export default _default;
export { parseDate, format };
