'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

/**
 * @module sDom
 * @since 0.0.3
 * @description sDom模块, 提供DOM相关处理
 * @example
 * // first import sDom
 * import { sDom } from 'js-simple-utils'
 * // then use sDom
 * onClick={sObject.stopPropagation}
 * onClick={sObject.preventDefault}
 * onClick={sObject.stopAndPrevent}
 */
/**
 * 阻止事件冒泡
 * @static
 * @since 0.0.3
 * @param {Event} e 事件对象
 * @returns {void}
 * @example
 * sDom.stopPropagation(e)
 */
const stopPropagation = (e) => {
    e.stopPropagation();
};
/**
 * 取消事件默认行为
 * @static
 * @since 0.0.3
 * @param {Event} e 事件对象
 * @returns {void}
 * @example
 * sDom.preventDefault(e)
 */
const preventDefault = (e) => {
    e.preventDefault();
};
/**
 * 阻止事件冒泡+取消事件默认行为
 * @static
 * @since 0.0.3
 * @param {Event} e 事件对象
 * @returns {void}
 * @example
 * sDom.stopAndPrevent(e)
 */
const stopAndPrevent = (e) => {
    stopPropagation(e);
    preventDefault(e);
};
var index = { stopPropagation, preventDefault, stopAndPrevent };

exports["default"] = index;
exports.preventDefault = preventDefault;
exports.stopAndPrevent = stopAndPrevent;
exports.stopPropagation = stopPropagation;
